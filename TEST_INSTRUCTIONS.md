# Инструкция по отладке "Нет данных"

## Шаг 1: Откройте консоль браузера

1. Откройте приложение: http://localhost:8081
2. Нажмите F12 (откроется DevTools)
3. Перейдите на вкладку "Console"

## Шаг 2: Проверьте загрузку данных

В консоли должны быть следующие сообщения:

### При загрузке страницы:
```
📥 Данные загружены: X.XX MB
🗜️ Compression: gzip
🔄 Конвертировано 84000 записей из ультра-компактного формата
```

**Если этого нет** - данные не загружаются. Проверьте:
- Запущен ли сервер: `python main.py`
- Доступен ли endpoint: http://localhost:8081/api/data/ultra?limit=1000000

## Шаг 3: Создайте панель

1. Нажмите "+ Добавить панель"
2. Выберите тип (Таблица или График)
3. Нажмите "Создать панель"
4. В открывшемся окне настроек:
   - Для **Таблицы**: добавьте хотя бы одну меру (Measures tab)
   - Для **Графика**: добавьте dimension (Dimensions tab) и measure (Measures tab)
5. Нажмите "Применить"

## Шаг 4: Проверьте логи в консоли

### Для таблицы вы должны увидеть:
```
📊 [Table] Получено записей: 84000
📊 [Table] Используем детальную таблицу
📊 [Table] Рендеринг: 84000 строк, X колонок
⚡ [Table] Используем виртуализацию для 84000 строк
✅ [Table] Виртуальная таблица создана успешно
```

### Для графика:
```
📈 [Chart] Получено записей: 84000
⚡ [Chart] Сэмплируем 84000 записей до 1000 точек
📈 [Chart] Создано групп: X
```

## Проблемы и решения

### Проблема 1: "VirtualTable is not defined"

**Симптом**:
```
❌ [Table] VirtualTable не загружен! Используем обычный рендеринг
```

**Решение**:
1. Проверьте что файл загружен:
   - В DevTools → Network → найдите `virtual-table.js`
   - Должен быть статус 200

2. Если файл не найден:
   ```bash
   # Проверьте что файл существует
   ls -la static/js/virtual-table.js

   # Перезапустите сервер
   python main.py
   ```

### Проблема 2: "Получено записей: 0"

**Симптом**:
```
⚠️ [Table] Нет данных для отображения
```

**Возможные причины**:
1. Данные не загрузились с сервера
2. Глобальные фильтры исключают все данные

**Решение**:
1. Проверьте в консоли общее количество:
   ```javascript
   // Вставьте в консоль:
   console.log('Total records:', window.app?.data?.length);
   ```

2. Если 0 - данные не загружены. Проверьте Network tab:
   - Найдите запрос к `/api/data/ultra`
   - Проверьте Response (должен быть JSON с данными)

3. Сбросьте фильтры:
   - Нажмите кнопку "Сбросить" в панели фильтров

### Проблема 3: "Показано X записей" но пустая таблица

**Симптом**: Счетчик показывает данные, но таблица пустая

**Решение**:
1. Откройте Elements tab в DevTools
2. Найдите элемент с классом `virtual-table-container`
3. Проверьте что внутри есть элементы

**Если элементов нет**:
```javascript
// В консоли выполните:
document.querySelector('.virtual-table-container')
// Должен вернуть элемент, не null
```

4. Если null - панель не создалась. Проверьте:
   - Добавили ли вы Measures в настройках панели?
   - Нет ли ошибок в консоли красного цвета?

### Проблема 4: График не строится

**Симптом**: Панель пустая, нет графика

**Решение**:
1. Проверьте что добавлены и Dimensions и Measures
2. В консоли проверьте:
   ```
   📈 [Chart] Создано групп: X
   ```
   Если X = 0, данные не сгруппировались

3. Попробуйте простую конфигурацию:
   - **Dimension**: timestamp (type: date)
   - **Measure**: status (aggregation: count)
   - **Chart type**: bar

## Быстрая проверка

Выполните в консоли:

```javascript
// 1. Проверка загрузки данных
console.log('Data loaded:', window.app?.data?.length || 0);

// 2. Проверка VirtualTable
console.log('VirtualTable available:', typeof VirtualTable !== 'undefined');

// 3. Проверка панелей
console.log('Active panels:', window.app?.panelManager?.panels?.size || 0);

// 4. Ручной тест таблицы
const testData = window.app?.data?.slice(0, 100) || [];
console.log('Test data:', testData.length, 'records');
if (testData.length > 0) {
    console.log('Sample record:', testData[0]);
}
```

**Ожидаемый результат**:
```
Data loaded: 84000
VirtualTable available: true
Active panels: 1  (или больше)
Test data: 100 records
Sample record: {timestamp: 1704067200, server_name: "SRV-001", ...}
```

## Если ничего не помогло

1. **Полная перезагрузка**:
   ```bash
   # Остановить сервер (Ctrl+C)
   # Перезапустить
   python main.py
   ```

2. **Жесткая перезагрузка браузера**:
   - Ctrl + Shift + R (Windows/Linux)
   - Cmd + Shift + R (Mac)

3. **Очистить кеш**:
   ```bash
   curl -X POST http://localhost:8081/api/cache/clear
   ```

4. **Проверить базу данных**:
   ```bash
   python test_performance.py
   ```
   Должно показать данные без ошибок.

5. **Проверить логи сервера**:
   В терминале где запущен сервер должно быть:
   ```
   ✅ В базе уже есть данные (1+ записей)
   ```

## Сбор информации для отладки

Если проблема не решается, соберите следующую информацию:

1. **Вывод консоли браузера** (скопируйте все сообщения)
2. **Логи сервера** (вывод в терминале)
3. **Network tab**: скриншот запроса к `/api/data/ultra`
4. **Результат быстрой проверки** (код выше)

## Контакты

Если ничего не помогло, отправьте собранную информацию с описанием проблемы.
